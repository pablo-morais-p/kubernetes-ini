apiVersion: v1
kind: Pod
metadata:
  name: app-noticias
  labels:
    app: portal-noticias
spec:
  containers:
    - name: portal-noticias
      image: aluracursos/portal-noticias:1
      envFrom:
        - configMapRef:
            name: portal-noticias-cm

