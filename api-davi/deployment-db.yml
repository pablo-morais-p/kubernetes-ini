apiVersion: apps/v1
kind: Deployment
metadata:
  name: deploy-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: db-api-davi
  template:
    metadata: 
      name: db-api-davi
      labels:
        app: db-api-davi
    spec:
      containers:
        - name: container-db
          image: postgres:17.5-alpine3.22
          ports:
            - containerPort: 5432
          envFrom:
          - secretRef:
              name: secret-db