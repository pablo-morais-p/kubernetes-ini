apiVersion: v1
kind: Pod
metadata:
  name: pod-apache
  labels:
    app: pod-apache
spec:
  containers:
  - name: container-apache
    image: ubuntu/apache2:latest
    ports:
      - containerPort: 80
