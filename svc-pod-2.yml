apiVersion: v1
kind: Service
metadata:
  name: svc-pod-2
spec:
  type: ClusterIP
  selector:
    app: pod-2
  ports:
  - port: 80 #porta onde o serviço escuta 
    #targetPort: 80 #porta para onde o serviço encaminha a requisição
